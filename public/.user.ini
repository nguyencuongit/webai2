; PHP upload limits for the Video Lab media endpoint.
; post_max_size must be larger than upload_max_filesize to include multipart data.
upload_max_filesize = 50M
post_max_size = 64M

; Allow enough time for larger video uploads on slower connections.
max_input_time = 300
max_execution_time = 300
